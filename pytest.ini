[pytest]
testpaths = codigo/tests
addopts = -q --strict-markers
markers =
    slow: pruebas costosas (perfilado de memoria, lotes grandes)
filterwarnings =
    ignore::FutureWarning
    ignore::UserWarning
    ignore::RuntimeWarning
